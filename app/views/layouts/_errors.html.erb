<% if thingy.errors.any? %>
  <div class="form-group">
    <div class="col-sm-10 col-sm-offset-2">
      <div class="alert alert-danger">
        <strong><%= pluralize(thingy.errors.count, "error") %> prohibited this post from being saved:</strong>

        <ul>
          <% thingy.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
<% end %>
